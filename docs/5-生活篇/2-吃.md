<script setup>
import LifeCardGrid from './_components/LifeCardGrid.vue'
import items from './_data/eat.json'

const filters = [
  {
    type: 'range',
    key: 'priceValue',
    label: '人均价格',
    min: 0,
    max: 200,
    step: 5,
    unit: '¥',
    maxLabel: '200+'
  },
  {
    type: 'choice',
    key: 'cuisine',
    label: '类别 / 菜系',
    auto: true
  }
]
</script>

# 吃

:::warning 说明
本页目前先使用 TODO 占位条目搭建结构，不填未经确认的真实店名、价格、路线或营业时间。
:::

<LifeCardGrid :items="items" :filters="filters" />
